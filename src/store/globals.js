import * as types from './mutation-types';

const state = {
    schemaVersion: "1.0.0",
    options: {
      globalState: true,
      reloadWindows: false,
      replaceAll: false,
    }
};

const getters = {};

const actions = {};

const mutations = {};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}